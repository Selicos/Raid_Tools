# Champion Prompt Template (Markdown)

> **Note:** For each section, pull in the corresponding information from the champion's JSON prompt file as defined in `Prompt_Template.json`. Use the bullet points as guidance for the level of detail and content expected.

---

## 0. Champion Name
Abbess

## 0.1 Owned Status
true

## 1. Base Stats
(Populate with the `base_stats` section from the JSON template)

## 2. Overview & Initial Summary
(Populate with the `overview` section from the JSON template)

## 3. Skill Summary & Rotation Analysis
(Populate with the `skills` section from the JSON template)

## 4. Skill Book Requirements & Effects
(Populate with the `books` section from the JSON template)

## 5. Aura Details
(Populate with the `aura` section from the JSON template)

## 6. AI Behavior & Skill Logic
(Populate with the `ai_logic` section from the JSON template)

## 7. Team Creator Inputs
(Populate with the `team_inputs` section from the JSON template)

## 8. Mastery Proc Simulation
(Populate with the `mastery_proc_simulation` section from the JSON template)

## 9. Mastery Recommendation
(Populate with the `recommended_mastery` section from the JSON template)

## 10. Clan Boss Damage Tracking
(Populate with the `clan_boss_damage` section from the JSON template)

## 11. Dungeon/Content Breakdown
(Populate with the `content_breakdown` section from the JSON template)

## 12. Ally Synergy & Speed Tuning
(Populate with the `synergy_speed` section from the JSON template)

## 13. Utility & Investment Value
(Populate with the `utility_investment` section from the JSON template)

## 14. Turn Meter Simulation & Gear Tradeoffs
(Populate with the `turn_meter_gear` section from the JSON template)

## 15. Final Summary
(Populate with the `final_summary` section from the JSON template)

## 16. Synergy Engine (Owned Champions Only)
(Populate with the synergy engine section from the JSON template, if applicable)

## 17. Community Ratings & Notes
(Populate with the `community` section from the JSON template)

---

For the canonical schema and field definitions, see: `ChampionIntake/templates/Prompt_Template.json`
